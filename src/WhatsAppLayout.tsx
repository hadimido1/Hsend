import React, { useState } from 'react';
import { Camera, MoreVertical, Search, MessageSquare, Users, Phone as PhoneIcon, Plus, Settings } from 'lucide-react';
import { useStore } from './lib/store';
import Sidebar from './components/Sidebar';
// We will move the tab logic here.
